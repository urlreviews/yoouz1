            {/* Business Dashboard View */}
            {activeSection === "business" && (
              <CopoBusinessDashboardView 
                onNavigate={setActiveSection}
              />
            )}

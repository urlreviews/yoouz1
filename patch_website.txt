                  {/* Website Line */}
                  <div className="px-5 py-3.5 flex items-center justify-between gap-3 hover:bg-zinc-50 transition-colors">
                    <div className="flex items-center gap-3 truncate w-full">
                      <Globe className="w-5 h-5 text-[#1a73e8] shrink-0" />
                      {hasUpgraded ? (
                        hasGenuineWebsite ? (
                          <a
                            href={place.website}
                            target="_blank"
                            rel="noreferrer"
                            className="text-xs text-[#1a73e8] hover:underline font-medium truncate"
                          >
                            {place.website?.replace(/^(https?:\/\/)?(www\.)?/, "").replace(/\/$/, "")}
                          </a>
                        ) : (
                          <span className="text-xs text-zinc-500">Website not provided</span>
                        )
                      ) : (
                        <div className="flex items-center justify-between w-full">
                          <span className="text-xs text-zinc-400 blur-[3px] select-none">www.example.com</span>
                          {isUserOwner && (
                            <button onClick={() => setIsPricingModalOpen(true)} className="text-[10px] bg-amber-50 text-amber-700 px-2 py-1 rounded-full border border-amber-200 font-bold flex items-center gap-1 shrink-0">
                              <Lock className="w-3 h-3" />
                              Unlock Link
                            </button>
                          )}
                        </div>
                      )}
                    </div>
                  </div>
